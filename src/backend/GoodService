import mysql.connector
from mysql.connector import Error
import AService

class GoodService(AService):
	def __init__(self):
		super().__init__()

	def get_goods_ids(self)->list:
		super().connect()
		super().disconnect()
		self.cursor.execute('SELECT TOP 20 id, mime_type FROM Good')